{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "api",
        "web",
        "scoring",
        "pipeline",
        "infra",
        "ci",
        "deps"
      ]
    ],
    "scope-empty": [0]
  }
}
